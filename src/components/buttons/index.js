import { Button } from "./Button";
import { IconButton } from "./IconButton";
import { MiniButton } from "./MiniButton";

export { Button, IconButton, MiniButton };

const buttons = { Button, IconButton, MiniButton };

export default buttons;